Metadata-Version: 2.4
Name: tw-stock-ai
Version: 0.1.0
Summary: Taiwan stock short-term screening and portfolio tracking system
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.116.0
Requires-Dist: uvicorn[standard]>=0.35.0
Requires-Dist: SQLAlchemy>=2.0.36
Requires-Dist: pydantic>=2.10.0
Requires-Dist: pydantic-settings>=2.7.0
Requires-Dist: jinja2>=3.1.5
Requires-Dist: python-multipart>=0.0.20
Requires-Dist: apscheduler>=3.10.4
Provides-Extra: dev
Requires-Dist: httpx>=0.28.1; extra == "dev"
Requires-Dist: pytest>=8.3.4; extra == "dev"

# 台股短線 AI 選股系統

這個專案是一套可部署到 GitHub / Zeabur 的台股短線選股與持股追蹤系統骨架，遵守以下原則：

- 不捏造股票、新聞、營收、財報或技術指標資料
- 不假設第三方 API 回傳欄位
- 沒有真實資料時，一律回傳 `missing` / `unavailable`
- 所有分數都可拆解成子分數與證據
- 所有結果都保留資料來源與抓取時間
- 先建立可擴展架構，再逐步接真實資料源

目前這一版先完成：

- 正式可執行的 FastAPI 後端
- SQLite / PostgreSQL 相容資料層
- 手動 CSV 匯入真實日線價格資料
- 短線 3-10 天選股引擎骨架
- 寶藏股欄位與缺資料保護
- 持股追蹤 API
- Discord Webhook 推播模組
- Web UI 儀表板
- Zeabur 可部署 Dockerfile

## 專案結構

```text
.
├─ data/
│  └─ import_templates/
├─ docs/
├─ scripts/
├─ src/
│  └─ tw_stock_ai/
│     ├─ adapters/
│     ├─ routers/
│     ├─ services/
│     ├─ static/
│     └─ templates/
├─ tests/
├─ Dockerfile
└─ pyproject.toml
```

## 目前資料策略

本專案目前不直接硬接任何第三方台股 API，原因是：

- 未在此工作階段內驗證第三方 API 欄位格式
- 不允許猜測欄位名稱或假設永遠穩定

因此第一版先提供：

- `manual_csv` adapter：匯入你已驗證來源的真實價格 CSV
- adapter 介面：後續可以安全接官方 / 券商 / 資料商來源

## CSV 匯入格式

請使用 [`data/import_templates/daily_prices.csv`](data/import_templates/daily_prices.csv) 作為欄位模板。

必要欄位：

- `symbol`
- `trade_date`
- `open`
- `high`
- `low`
- `close`
- `volume`
- `source_name`
- `source_url`
- `fetched_at`

## 本機啟動

```bash
python -m venv .venv
.venv\Scripts\activate
pip install -e .
uvicorn tw_stock_ai.main:app --reload
```

預設網址：

- API: `http://127.0.0.1:8000`
- UI: `http://127.0.0.1:8000/`

## 環境變數

請參考 [`.env.example`](.env.example)。

## Discord 排程

系統可在週一到週五上午 8:00（Asia/Taipei）執行：

1. 讀取最新真實價格資料
2. 重新計算選股
3. 將前 5 名推播到 Discord

若資料不足，則不推播虛假名單，只回報缺資料狀態。

## API 重點

- `GET /api/health`
- `POST /api/ingestion/manual/prices`
- `POST /api/screenings/run`
- `GET /api/screenings/latest`
- `GET /api/holdings`
- `POST /api/holdings`

## Zeabur 部署

本專案使用 `Dockerfile` 部署，Zeabur 可直接從 GitHub 匯入。
